Tarak's dotfiles

notes:

For ACP debugging, two things that worked well: pipe JSON-RPC lines straight
into pi-acp-server.sh from the shell to isolate bridge vs. editor, and check
~/.local/state/nvim/codecompanion.log (you can raise verbosity with
opts.log_level = "DEBUG" in the codecompanion setup).

